# requirements.txt for local development and testing
# Note: Lambda and Cloud Functions have these available by default
# or will install from pyproject.toml dependencies

# Core dependencies (minimal for production runtime)
pydantic>=2.8.0,<3
python-json-logger>=2.0.7,<3

# Cloud SDK dependencies (optional for local testing)
# boto3>=1.34.0  # AWS SDK - available in Lambda runtime
# google-cloud-secret-manager>=2.20.0  # GCP SDK - available in Function runtime

# Development and testing dependencies
pytest>=8.2.0,<9
pytest-cov>=5.0.0,<6
mypy>=1.11.0,<2
ruff>=0.6.2,<1
black>=24.8.0
types-requests

# Optional: for local Splunk HEC testing
# requests>=2.32.0,<3